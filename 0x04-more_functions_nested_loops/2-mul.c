#include "main.h"
#include <stdio.h>
#include <math.h>

/**
 * mul - multiplies two integers
 * @x: first integer
 * @y: second integer
 * @z: the product of two integers
 * Return: returns an integer value z
 */

int mul(int x, int y)
{
	int z;

	z = x * y;
	return (z);
}

